import 'package:flutter/material.dart';

class InterestScreen extends StatelessWidget {
  const InterestScreen({super.key});

  static final routeName = '/interestScreen';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
